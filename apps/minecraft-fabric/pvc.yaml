apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minecraft-fabric-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  volumeName: minecraft-fabric-data-pv