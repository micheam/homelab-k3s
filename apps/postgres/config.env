# Shared PostgreSQL Configuration
#
# Each service uses its own schema (e.g., limno.notes).
# To add a new service, create a user and schema:
#   CREATE USER myservice WITH PASSWORD 'xxx';
#   CREATE SCHEMA myservice AUTHORIZATION myservice;
POSTGRES_USER=postgres
