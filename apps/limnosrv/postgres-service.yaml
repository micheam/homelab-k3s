apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: limno-postgres
spec:
  type: ClusterIP
  selector:
    app: limno-postgres
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
