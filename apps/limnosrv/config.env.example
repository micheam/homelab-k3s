# limnosrv Configuration
# Copy this file to config.env and modify values
#
# DATABASE_URL: shared PostgreSQL (postgres namespace)
# Password must match the limno user created in PostgreSQL.

DATABASE_URL=postgres://limno:changeme@postgres.postgres.svc.cluster.local:5432/limno?sslmode=disable
LISTEN_ADDR=:8080
