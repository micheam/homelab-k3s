# limnosrv Configuration
# Copy this file to config.env and modify values

# PostgreSQL
POSTGRES_USER=limno
POSTGRES_PASSWORD=changeme
POSTGRES_DB=limno

# limnosrv
DATABASE_URL=postgres://limno:changeme@postgres:5432/limno?sslmode=disable
LISTEN_ADDR=:8080
